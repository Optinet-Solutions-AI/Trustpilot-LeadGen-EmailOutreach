{
  "version": 2,
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "out" }
    }
  ],
  "rewrites": [
    { "source": "/leads/:id", "destination": "/leads/_id" }
  ]
}
