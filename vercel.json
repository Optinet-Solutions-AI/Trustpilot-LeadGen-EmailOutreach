{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "frontend/out" }
    }
  ],
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://trustpilot-gateway-3lazv1k9.uc.gateway.dev/api/:path*" },
    { "source": "/leads/:id", "destination": "/leads/_id" }
  ]
}
